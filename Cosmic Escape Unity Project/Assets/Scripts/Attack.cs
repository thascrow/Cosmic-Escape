using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Attack : MonoBehaviour
{
    GameObject characterAttacking;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    //oncoll

    // Update is called once per frame
    void Update()
    {
        
    }
}
